# CodeEval Design System

> 本文档是 CodeEval Web 界面的唯一设计基准。新增页面、组件和交互应先更新本文档，再进入实现。

## 0. 文档状态

- 版本：`1.3`
- 状态：实施中
- 更新日期：2026-09-18
- 适用范围：教师端、学生端、登录页，以及桌面与移动端界面
- 产品定位：面向编程课程的作业发布、代码提交与评估反馈工具

### 变更记录

| 日期 | 版本 | 变更 |
| --- | --- | --- |
| 2026-09-18 | 1.0 | 建立全量重构的设计基线 |
| 2026-09-18 | 1.1 | 完成 tokens、导航框架、教师工作台与桌面列表的第一轮实现 |
| 2026-09-18 | 1.2 | 增加长表单目录与固定提交操作；移动端聚焦模式隐藏全局导航；作业要求默认折叠；编辑模式使用明确只读信息替代禁用表单控件 |
| 2026-09-18 | 1.3 | 参考木犀茶馆的暖色、软质卡片和悬浮移动导航，改为暖橙品牌色与桌面顶部导航，同时保持教学工作台的信息架构 |

---

## 1. 设计目标

CodeEval 应当像一件可靠、安静、可以每天使用的教学工具。界面需要帮助教师快速处理课程事务，也需要让学生看懂评分依据和下一步改进方向。

设计优先级从高到低为：

1. 信息是否清楚、可判断、可操作
2. 评分和评估过程是否可信、可追溯
3. 教师和学生是否能快速找到下一步动作
4. 界面是否一致、易学、适合长期使用
5. 品牌表达是否克制而有辨识度

### 设计原则

#### 1. 任务优先，不做口号型首页

首屏优先展示待处理事项、截止时间、评估状态和异常情况。避免用大面积 Hero、欢迎语或装饰性口号占据核心空间。

#### 2. 证据优先，不神化 AI

AI 是评估方法之一，不是界面的主角。展示结果时先说结论，再说明测试、规则、代码事实和评分依据。避免使用发光、渐变、机器人图标等“AI 感”装饰。

#### 3. 少用容器，多用结构

不要让每一组内容都变成独立大卡片。优先使用标题层级、留白、分割线、表格和列表组织信息。卡片只用于需要明确边界或独立操作的内容。

#### 4. 状态必须可以被快速扫读

开放、已截止、评估中、失败、待复核等状态使用一致的颜色、标签和文字。不能只依赖颜色表达状态。

#### 5. 桌面高效，移动端完整

桌面端强调信息密度和批量操作；移动端重新组织内容，而不是把桌面表格塞进横向滚动区域。

---

## 2. 视觉方向

### 关键词

- 克制
- 学术
- 清晰
- 可信
- 高信息密度
- 温暖但不幼稚

### 避免的视觉语言

- 大面积深色 Hero
- 荧光色作为主要品牌特征
- 无意义的英文大写眉题
- 无差别使用超大圆角和层层嵌套卡片
- 正文内容上的玻璃拟态、彩色渐变和发光效果
- `</>`、`⌂`、`▤` 等字符充当正式图标
- 为了“高级感”而降低文字对比度
- 到处使用“智能”“AI 驱动”“赋能”等宣传型表达

### 整体气质

使用略带暖意的浅灰作为页面底色，深墨色承载主要文字，琥珀橙作为品牌色。品牌色只出现在导航选中态、关键操作和少量数据强调中。顶栏、移动端导航允许使用低透明度和背景模糊，正文面板保持实体白色。整体应当像温暖、轻巧的校园工具，而不是论坛复刻或 SaaS 营销网站。

木犀茶馆仅作为视觉节奏参考：吸收其暖橙色、圆润操作控件、柔和层级和移动端应用感；不复制论坛的帖子流、分类入口、头像主导的信息结构。

---

## 3. Design Tokens

所有颜色、字号、间距、圆角和阴影必须通过 CSS 变量维护，业务样式中不新增散落的魔法值。

### 3.1 颜色

```css
:root {
  /* Surface */
  --color-canvas: #f7f8fb;
  --color-surface: #ffffff;
  --color-surface-subtle: #f2f3f6;
  --color-surface-raised: #ffffff;

  /* Text */
  --color-text: #1a202c;
  --color-text-secondary: #646b76;
  --color-text-tertiary: #9298a2;
  --color-text-inverse: #ffffff;

  /* Border */
  --color-border: #e8e9ed;
  --color-border-strong: #d9dbe1;

  /* Brand */
  --color-brand: #ee8a00;
  --color-brand-hover: #d97800;
  --color-brand-subtle: #fff2d7;

  /* Semantic */
  --color-success: #a66300;
  --color-success-bg: #fff2d7;
  --color-warning: #9a6712;
  --color-warning-bg: #fff5dc;
  --color-danger: #a33d36;
  --color-danger-bg: #faeae8;
  --color-info: #3f617b;
  --color-info-bg: #eaf0f5;
}
```

使用规则：

- 页面背景使用 `canvas`，主要内容使用 `surface`。
- 正文对比度不得低于 WCAG AA。
- 品牌色不用于大面积背景。
- 分数颜色仅用于提示区间，数字本身同时保留文字含义。
- 绿色仅用于高分区间；开放、完成、有效、成功提示等正向状态使用琥珀色或中性色，不再复用绿色。
- “评估失败”和“低分”不是同一语义，不共用一种状态标签。

### 3.2 字体

```css
--font-sans: Inter, "PingFang SC", "Microsoft YaHei", sans-serif;
--font-mono: "DM Mono", "SFMono-Regular", Consolas, monospace;
```

- 产品文字统一使用无衬线字体。
- 代码、语言标识、测试输入输出使用等宽字体。
- 英文和数字使用 Inter；中文跟随系统中文字体。
- 不再用 Arial 覆盖全局字体。

### 3.3 字号

| Token | 字号/行高 | 用途 |
| --- | --- | --- |
| Display | 32/40 | 页面核心数字或登录页品牌句，少量使用 |
| H1 | 28/36 | 页面标题 |
| H2 | 20/28 | 区块标题 |
| H3 | 16/24 | 卡片或列表组标题 |
| Body | 14/22 | 默认正文 |
| Small | 12/18 | 辅助说明、元数据 |
| Micro | 11/16 | 标签内部，禁止用于长句 |

移动端 H1 为 `24/32`。正文最小字号为 12px，表单和主要列表不得低于 14px。

### 3.4 间距

使用 4px 基础网格：

```css
--space-1: 4px;
--space-2: 8px;
--space-3: 12px;
--space-4: 16px;
--space-5: 20px;
--space-6: 24px;
--space-8: 32px;
--space-10: 40px;
--space-12: 48px;
```

- 页面区块间距：32px
- 区块标题与内容：16px
- 表单字段间距：20px
- 列表行垂直内边距：14–16px
- 不因内容少而人为放大卡片高度

### 3.5 圆角和阴影

```css
--radius-sm: 10px;
--radius-md: 18px;
--radius-lg: 24px;
--shadow-card: 0 10px 28px rgb(16 24 40 / 6%);
--shadow-popover: 0 12px 34px rgb(16 24 40 / 12%);
--shadow-dialog: 0 20px 54px rgb(16 24 40 / 16%);
```

- 输入框、按钮：10–13px；分段控件和搜索框允许使用胶囊圆角。
- 卡片、面板：18px；移动端独立任务卡可使用 20–22px。
- 对话框：24px。
- 普通卡片使用极弱边框和一级软阴影，不叠加多层卡片。
- 强阴影只用于弹层、菜单和悬浮导航。

### 3.6 图标

- 统一使用一套 20px 线性 SVG 图标。
- 常规描边宽度 1.75px。
- 图标与文字同时出现时，图标不承担唯一语义。
- 不使用 Unicode 字符模拟图标。
- 产品标记统一使用 `{}` 代码符号；浏览器 favicon 使用同一符号的琥珀渐变 SVG，不再使用字母 `C`。

---

## 4. 页面框架

### 4.1 桌面端

适用宽度：`>= 1024px`

```text
┌───────────────────────────────────────────────────────┐
│ CodeEval   工作台  作业  提交             用户 / 退出 │
├───────────────────────────────────────────────────────┤
│ 页面标题                              主要操作        │
│                                                       │
│ 页面内容                                              │
└───────────────────────────────────────────────────────┘
```

- 顶部全局导航高度：74px，使用暖白半透明背景和轻量模糊。
- 导航内容与页面内容共用 1240px 对齐基线。
- 内容最大宽度：1240px。
- 内容区左右内边距：32px；大屏为 40px。
- 页面标题区域不使用卡片背景。
- 用户信息和退出操作放在顶栏右侧。

### 4.2 平板端

适用宽度：`768–1023px`

- 保持顶部导航；压缩导航间距和文字，不切换为图标侧栏。
- 页面左右内边距 24px。
- 双栏区域降为单栏或 60/40 布局。
- 表格保留核心列，次要信息进入行展开区。

### 4.3 移动端

适用宽度：`< 768px`

- 顶部使用 52px App Bar：返回/菜单、页面名、当前主要操作。
- 一级导航使用距屏幕边缘 14px 的悬浮胶囊底部导航，最多 4 项。
- 页面左右内边距 16px。
- 桌面表格必须转换为信息卡片列表。
- 主要按钮在需要时固定于底部安全区域。
- 保留产品身份；课程上下文仅在接入真实课程数据后展示。
- 禁止依赖横向滚动完成主要任务。

---

## 5. 信息架构

教师端一级导航：

1. 工作台
2. 作业
3. 提交
4. 课程设置

“发布作业”是作业页面的主要操作，不作为独立一级导航。

学生端一级导航：

1. 首页
2. 作业
3. 提交记录
4. 我的学习

### 全局信息层级

```text
课程
└── 作业
    ├── 题目与要求
    ├── 测试用例
    ├── 评分量规
    └── 学生提交
        ├── 代码
        ├── 沙箱测试
        ├── 静态分析
        ├── 模型评估
        └── 最终反馈
```

界面应该始终让用户知道自己位于哪门课程、哪项作业和哪次提交中。

---

## 6. 页面设计

### 6.1 登录页

目标：快速进入系统，同时建立可信、克制的产品印象。

桌面端采用 40/60 分栏：

- 左侧：Logo、产品名、简短定位、三项产品能力。
- 右侧：宽度 360–400px 的登录表单。
- 背景使用暖灰，不使用整屏深绿色块。
- 左右分栏共用连续背景，不绘制中间分隔线或产生明显色块接缝。

文案建议：

- 产品名：`CodeEval`
- 定位：`编程作业与评估工作台`
- 说明：`发布作业、检查运行结果，并给出有依据的学习反馈。`

表单结构：

1. 登录标题
2. 账号
3. 密码与显示/隐藏按钮
4. 登录按钮
5. 错误提示
6. 开发环境下的“使用演示账号”折叠区域

演示账号只在开发或演示环境展示，生产环境隐藏。移动端保留 Logo 和一句定位，不把品牌区完全删除。

### 6.2 教师工作台

目标：回答“现在最需要处理什么”。

页面顺序：

1. 页面标题和课程范围
2. 待处理事项
3. 课程概览
4. 最近提交
5. 作业进度和班级问题

#### 待处理事项

最多展示 3 条，按紧急程度排序：

- 评估失败，需要重新运行
- 即将截止但提交率低
- 存在大量相同错误
- 新提交待查看

无待处理事项时显示一行轻量空状态，不保留大空卡片。

#### 课程概览

使用一条横向数据栏，不使用三张独立大卡：

- 进行中作业
- 本周提交
- 平均得分
- 待复核

每项都可以进入相应筛选结果。

#### 最近提交

显示学生、作业、提交时间、状态、分数。整行可进入详情。最多展示 6 条。

### 6.3 作业列表

桌面端使用表格，默认列：

1. 作业名称与语言
2. 截止时间
3. 已提交/总人数
4. 平均分
5. 状态
6. 更多操作

页面主要操作为“发布作业”。状态修改放入行操作菜单，不在每行常驻下拉框。

筛选项：状态、语言、发布时间；搜索匹配名称和描述。

移动端每项显示：

- 作业名 + 状态
- 截止时间
- 提交进度 + 平均分
- 详情入口

### 6.4 发布/编辑作业

采用分步骤或分区长表单：

1. 基本信息
2. 编程要求
3. 测试用例
4. 评分量规
5. 评估方式
6. 发布确认

桌面端左侧显示表单，右侧显示步骤导航和完成状态。移动端使用顶部步骤条。

关键规则：

- 自动保存草稿，并明确显示保存状态。
- 测试用例生成结果只是草稿，必须由教师确认。
- 危险操作与主要操作分开。
- 发布前显示摘要：截止时间、语言、总分、测试数量、是否启用模型评估。
- 编辑已发布作业时明确提示对既有提交的影响。

### 6.5 提交列表

桌面端表格默认列：

1. 学生
2. 作业
3. 提交时间
4. 运行状态
5. 评估状态
6. 得分
7. 更多操作

运行状态和评估状态必须拆开，不能用一个“已评估”覆盖全部过程。

支持：

- 按作业、状态、分数区间筛选
- 搜索学生或作业
- 按时间和得分排序
- 失败提交快速筛选

移动端卡片显示学生、作业、时间、两个状态和得分。

### 6.6 提交详情

目标：让教师和学生理解结果，而不只是看到一个分数。

页面头部：

- 作业名称
- 学生与提交时间
- 评估状态
- 最终得分
- 教师操作：复核、重新评估、导出（按权限显示）

主体采用两个页签：

#### 反馈

顺序固定为：

1. 总结
2. 优先改进项
3. 做得好的地方
4. 分项评分
5. 测试结果
6. 评估证据

“问题点”和“优化方向”合并为带优先级的改进项，避免重复表达。

分项评分默认展示得分、量规和一句理由；详细证据按需展开。没有证据时不展示“置信度 0%”等内部字段，而显示“暂无可验证证据”。

#### 代码

- 使用真正的代码编辑器只读模式。
- 支持行号、语法高亮、换行开关和复制。
- 评估意见可定位到相关代码行。
- 移动端代码区域可横向滚动，但页面主体不能整体横向滚动。

### 6.7 学生首页

目标：回答“接下来做什么”和“最近哪里有进步”。

页面顺序：

1. 最近截止的作业
2. 需要继续修改的提交
3. 最近反馈
4. 学习趋势

不使用泛化欢迎语。学习趋势只在数据量足够时展示，少于 3 次有效提交时显示解释性空状态。

### 6.8 学生作业详情与提交

- 题目要求与提交区分栏展示，移动端上下排列。
- 编辑器上方明确显示语言和运行约束。
- 提交前可以运行公开测试。
- 提交按钮显示剩余提交次数或策略。
- 排队、运行、评估分别显示进度。
- 失败信息应告诉学生下一步可执行动作。

---

## 7. 核心组件规范

### 7.1 Button

类型：

- Primary：每个页面或弹层最多一个
- Secondary：普通操作
- Tertiary：低强调文字操作
- Danger：删除、关闭提交等危险操作

尺寸：

- 默认高度 36px
- 大尺寸 40px，仅用于登录和关键提交
- 移动端触控区域不小于 44×44px

加载时保留按钮宽度，显示 spinner 和进行中的动词。禁用态不能用于替代错误解释。

### 7.2 Status Badge

所有状态使用“颜色 + 文本”。

| 类别 | 示例 |
| --- | --- |
| 成功 | 已完成、测试通过、已评估 |
| 警告 | 即将截止、待复核、部分通过 |
| 危险 | 运行失败、评估失败、已逾期 |
| 信息 | 排队中、运行中、评估中 |
| 中性 | 草稿、已关闭、未提交 |

标签不得使用纯大写英文。

### 7.3 Table

- 表头固定在滚动容器顶部。
- 行高不低于 52px。
- 主列左对齐，数字列右对齐，状态列左对齐。
- 行点击与内部按钮区域不能互相冲突。
- 排序列必须显示排序方向。
- 无数据、加载失败、筛选无结果分别设计。

### 7.4 Form

- 标签位于控件上方。
- 必填信息使用文字说明或统一标记。
- 帮助文字紧邻字段。
- 错误在字段下方出现，并说明如何修正。
- 不使用 placeholder 替代标签。
- 日期、分数、次数等字段必须有格式或范围说明。

### 7.5 Empty State

空状态包含：

1. 发生了什么
2. 用户可以做什么
3. 可选的单个操作

不使用抽象插画填充空间。加载中使用骨架屏或行内进度，不使用空状态组件。

### 7.6 Feedback Block

反馈块类型：总结、优点、改进、证据、教师备注。

- 默认使用白色背景和左侧语义色标识。
- 不使用整块浅红/浅绿背景堆叠。
- 每条反馈尽量对应一个可定位的事实。
- 改进项使用明确动词开头，例如“检查空输入”“补充复杂度说明”。

### 7.7 Toast 与 Alert

- Toast 用于短暂成功反馈，不承载关键错误。
- Alert 用于页面级错误、权限问题或无法继续的状态。
- 错误信息包含原因、影响和恢复操作。

---

## 8. 文案规范

### 语气

清楚、直接、尊重用户专业判断。避免过度热情、拟人化和营销语言。

推荐：

- `3 份提交等待查看`
- `测试运行失败，请检查标准输入格式。`
- `评估依据不足，建议教师复核。`
- `发布作业`

避免：

- `今天也一起把反馈做得更好。`
- `AI 正在为你赋能`
- `太棒了！你的代码表现非常出色！`
- `COURSE OVERVIEW`
- `AI ASSESSMENT`

### 命名统一

- 一级导航使用“提交”，页面标题可使用“提交记录”。
- 教师创建内容统一称为“发布作业”，未发布状态称为“草稿”。
- 系统执行代码称为“运行”或“测试运行”。
- 综合生成结果称为“评估”，人工动作称为“复核”。
- 不混用“评分”“评价”“评测”表示同一流程。

### 时间与数字

- 同一年：`9 月 18 日 17:05`
- 跨年：`2027 年 1 月 3 日 09:00`
- 相对时间只用于辅助，例如 `2 小时前`，同时可查看准确时间。
- 分数写作 `92 / 100`，不要仅展示孤立的 `92`。
- 数量使用完整单位，例如 `3 份提交`、`2 项作业`。

---

## 9. 交互与状态

每个数据页面必须覆盖：

1. 首次加载
2. 加载成功
3. 空数据
4. 筛选无结果
5. 请求失败
6. 权限不足
7. 局部更新中
8. 更新成功或失败

### 评估流程状态

```text
已提交 → 排队中 → 运行测试 → 静态分析 → 模型评估 → 已完成
                      └──────────────→ 失败 / 待复核
```

- 用户看到的状态必须来自真实流程阶段。
- 不确定耗时时使用阶段状态，不展示虚假的百分比进度。
- 重新评估不会覆盖历史结果，应保留版本和时间。

### 危险操作

关闭提交、删除草稿、重新评估等操作必须说明影响范围。确认框按钮使用具体动词，例如“关闭提交”，不用“确定”。

---

## 10. 可访问性

- 正文与背景对比度达到 WCAG AA。
- 所有操作支持键盘访问。
- 焦点环清晰可见，不因鼠标操作永久移除。
- 图标按钮必须提供可访问名称和 Tooltip。
- 状态不只依赖颜色。
- 表单错误通过 `aria-describedby` 与字段关联。
- 动态评估状态通过适度的 live region 通知，不频繁打断。
- 尊重 `prefers-reduced-motion`。
- 页面缩放至 200% 时仍可完成主要任务。

---

## 11. 动效

动效只用于解释状态变化：

- 按钮与菜单：120–160ms
- 面板与对话框：180–220ms
- easing：`cubic-bezier(0.2, 0, 0, 1)`
- 骨架屏使用低对比度渐变
- 禁止大面积入场动画、数字滚动和装饰性浮动

---

## 12. 实现约束

- 现有页面允许重构 DOM，不要求兼容旧 CSS 类名。
- 先建立 tokens 和基础组件，再重做具体页面。
- 不引入重量级 UI 组件库；图标可以引入轻量 SVG 图标包。
- 页面业务状态与表现组件分离。
- 响应式行为由组件明确维护，不通过隐藏重要信息解决。
- 所有新组件至少验证 360px、768px、1024px、1440px 四种宽度。
- 所有页面需提供加载、空数据和错误状态。

推荐目录：

```text
design.md
src/
├── components/
│   ├── layout/
│   ├── navigation/
│   ├── data-display/
│   ├── feedback/
│   ├── forms/
│   └── ui/
├── pages/
│   ├── auth/
│   ├── teacher/
│   └── student/
└── styles/
    ├── tokens.css
    ├── reset.css
    └── globals.css
```

`design.md` 当前位于项目根目录，后续不在 `src` 内复制第二份。

---

## 13. 重构顺序

### Phase 1：基础系统

- 建立颜色、字体、间距、圆角 tokens
- 建立 AppShell、页面标题、按钮、输入框、状态标签、空状态
- 接入统一 SVG 图标
- 删除旧的全局覆盖和重复样式

### Phase 2：导航与核心列表

- 重做桌面侧栏、顶栏和移动端底部导航
- 重做教师工作台
- 重做作业列表
- 重做提交列表
- 完成移动端卡片式降级

### Phase 3：关键任务

- 重做发布/编辑作业流程
- 重做学生作业与代码提交
- 补齐自动保存、运行与评估状态

### Phase 4：反馈体验

- 重做提交详情
- 建立反馈、评分、证据和代码定位组件
- 区分教师复核与自动评估结果

### Phase 5：登录与收尾

- 重做登录页
- 统一文案和空状态
- 完成可访问性与响应式验收
- 清理全部旧样式和失效组件

---

## 14. 页面验收清单

每个页面完成前检查：

- [ ] 用户能在 5 秒内判断页面用途和下一步动作
- [ ] 首屏没有无业务价值的大面积装饰
- [ ] 主操作只有一个且位置稳定
- [ ] 重要状态同时包含颜色和文字
- [ ] 不存在为了布局而使用的空白大卡片
- [ ] 360px 宽度下无页面级横向滚动
- [ ] 表格在移动端已经转换为可读列表
- [ ] 加载、空数据、无结果和错误状态均可用
- [ ] 键盘可以完成核心任务
- [ ] 正文与辅助文字对比度合格
- [ ] 文案没有无意义英文、宣传话术和 AI 拟人化
- [ ] 评估结论可以追溯到测试、规则或代码事实
- [ ] 页面样式只使用已定义 tokens

---

## 15. 当前决策

以下决策在后续重构中默认生效：

- 删除现有工作台 Hero。
- “发布作业”从一级导航移入作业页面。
- 移动端表格改为卡片列表。
- 删除装饰性英文眉题。
- Unicode 字符图标替换为统一 SVG 图标。
- 提交详情以“反馈 / 代码”双页签组织。
- AI 相关信息进入评估方法与证据层，不作为页面品牌装饰。
- 演示账号入口只在开发或演示环境展示。
- 不保留旧版视觉兼容层。
- 发布和编辑作业属于聚焦任务流：移动端隐藏底部全局导航，使用明确返回入口和固定保存/发布按钮。
- 发布作业桌面端使用左侧粘性目录，长表单按基本信息、资料、量规、测试和评估方式组织。
- 学生移动端进入代码提交页时，作业要求和评分标准默认折叠，优先让编辑器进入首屏后的可见区域。
- 学生首页以“可继续提交”和真实低分记录为统计口径，不把全部开放作业等同于未完成作业。
- 编辑模式中不可修改的字段应渲染为只读信息行，不使用灰色禁用输入框或禁用下拉框伪装成可编辑控件。
- 教师端参考实现使用与学生提交一致的代码编辑器，跟随语言提供行号、语法高亮、格式化、复制和全屏能力。
- 在后端课程实体与课程选择能力完成前，不展示写死的课程名称、学期或“当前课程”模块。
